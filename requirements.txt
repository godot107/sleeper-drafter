# --- Core: HTTP + data ---
requests>=2.32          # Sleeper API client; needs an explicit User-Agent (see sleeper_client.py)
pandas>=2.2             # player/projection frames, the draft board
numpy>=1.26             # vectorised survival + VONA math (must stay <200ms per recompute)

# --- Terminal UI ---
rich>=13.7              # rich.Live dashboard during the draft

# --- Browser dashboard (optional; the terminal UI works without it) ---
dash>=2.17              # interactive dashboard; bundles plotly + flask

# --- Validation ---
pydantic>=2.7           # validates Sleeper API payloads (shapes are inconsistent — see README)

# --- Dev (optional) ---
pytest>=8.0             # pick arithmetic + probability invariants
